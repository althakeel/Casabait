# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files — never commit credentials
.env
.env.local
.env*.local
.env.production

# email.json uses ${VAR} placeholders only (safe to commit).
# Real SMTP credentials belong in .env / .env.local (gitignored above).

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
